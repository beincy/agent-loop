# GUI 样式优化说明

## 本次优化

### 1. 统一按钮尺寸
所有按钮统一高度为 **28px**，宽度根据功能调整：

- **"+ 新建"** 按钮：60x28px
- **"🗑 删除"** 按钮：60x28px  
- **"💾 保存配置"** 按钮：90x28px
- **"▶ 启动服务" / "🛑 停止服务"** 按钮：100x28px
- **"取消"** 按钮：60x28px
- **"✅ 创建"** 按钮：80x28px

**改进前**：
- 部分按钮没有固定尺寸（自动大小）
- 启动按钮 120x32px，其他按钮大小不一

**改进后**：
- 所有按钮高度统一为 28px
- 宽度根据文本内容合理分配
- 视觉上更加整齐统一

### 2. 优化单行输入框高度
所有单行文本输入框**不再固定高度**，使用 egui 默认行高：

```rust
// 改进前
ui.add_sized([ui.available_width(), 32.0],
    egui::TextEdit::singleline(&mut text));

// 改进后
ui.text_edit_singleline(&mut text);
```

**涉及字段**：
- WS 地址
- 默认工作区
- 订阅名称
- Smee URL
- 工作区（可选）
- 正则表达式

**改进效果**：
- 从视觉上的"2行高"变为真正的单行高度
- 输入框更加紧凑，不会显得臃肿
- 与标签的间距更协调

### 3. 保留多行文本框
**基础提示词** 仍然保持多行文本框，高度 100px：

```rust
ui.add_sized(
    [ui.available_width(), 100.0],
    egui::TextEdit::multiline(&mut base_prompt)
);
```

这是唯一需要多行输入的字段，保持原样。

## 优化对比

| 元素 | 改进前 | 改进后 |
|------|--------|--------|
| 按钮高度 | 混乱（自动/32px/无） | 统一 28px |
| 单行输入框 | 32px（显示2行高） | 自动（单行高度） |
| 多行提示词 | 100px | 100px（保持） |
| 视觉一致性 | ❌ 不一致 | ✅ 统一 |

## 技术细节

### 按钮统一使用 add_sized
```rust
if ui.add_sized([宽度, 28.0], egui::Button::new("文本")).clicked() {
    // 处理点击
}
```

### 输入框区分单行和多行
```rust
// 单行：使用默认高度
ui.text_edit_singleline(&mut text);

// 多行：指定高度
ui.add_sized([ui.available_width(), 100.0],
    egui::TextEdit::multiline(&mut text));
```

## 视觉改进

改进后的界面具有以下特点：

1. **按钮整齐划一**
   - 所有按钮高度相同
   - 主要操作按钮稍宽（启动/保存）
   - 次要操作按钮适中（新建/删除）

2. **输入框简洁干净**
   - 单行输入框真正"单行"
   - 不会有"看起来像2行"的违和感
   - 整体更紧凑、更专业

3. **间距协调**
   - 标签与输入框之间自然衔接
   - 不会因为输入框过高而产生大量留白
   - 视觉重心更稳定

## 编译和测试

```bash
cd rust
cargo build --release
./target/release/agent-loop gui
```

编译成功，GUI 运行正常。

## 后续建议

如果需要进一步优化：

1. **自定义主题**
   - 设置统一的按钮圆角
   - 调整按钮 hover/active 状态颜色
   - 统一卡片边框和阴影

2. **响应式尺寸**
   - 根据窗口宽度动态调整按钮宽度
   - 小窗口时按钮文本可能需要缩写

3. **输入框增强**
   - 添加占位符文本（placeholder）
   - 输入验证提示（红色边框）
   - 必填字段标记（*）
