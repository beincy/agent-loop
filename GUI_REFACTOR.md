# GUI 重构说明

## 改进内容

### 1. 顶部状态栏
- 标题 "Agent Loop" 显示在左侧
- 运行状态指示器显示在右侧（🟢 运行中 / ○ 已停止）
- 取消了原来混在中间的状态显示

### 2. 左右分栏布局
- **左侧面板**：订阅列表（280px宽，可调整至200-400px）
  - 标题 "订阅列表" + "新建" 按钮
  - 可选择的订阅项列表
  - 显示启用/禁用状态
  - 空状态提示
  
- **右侧面板**：配置和详情
  - 连接配置卡片（WS地址 + 默认工作区）
  - 订阅详情卡片（选中订阅时显示）
  - 空状态提示（未选择时）

### 3. 新建订阅弹窗
- 点击左上角 "+ 新建" 按钮触发
- 居中模态窗口
- 宽度 480px
- 包含所有必要字段
- "取消" 和 "创建" 按钮

### 4. 多行提示词输入
- 基础提示词改为多行文本框
- 高度 100px
- 支持换行和滚动

### 5. 输入框高度优化
- 所有单行输入框：32px
- 多行文本框：100px
- 下拉框：自动高度，宽度占满

### 6. 底部操作栏
- 左侧：次要按钮 "💾 保存配置"
- 右侧：主要按钮 "▶ 启动服务" 或 "🛑 停止服务"（120x32px）
- 状态消息显示在按钮旁边

### 7. 卡片化设计
- 使用 `egui::Frame::group()` 创建卡片
- 内边距 16px
- 自动边框和背景

### 8. 间距优化
- 区域间距：16px
- 元素间距：8px
- 标签与输入框间距：4px
- 顶部/底部内边距：8px

## 技术细节

### 状态管理
```rust
pub struct AgentLoopApp {
    // ... 原有字段 ...
    selected_subscription: Option<usize>,      // 当前选中的订阅索引
    show_new_subscription_dialog: bool,        // 是否显示新建弹窗
}
```

### 布局结构
```
TopBottomPanel::top("top_bar")           // 顶部状态栏
  └─ 标题 + 状态指示器

TopBottomPanel::bottom("bottom_bar")     // 底部操作栏
  └─ 保存按钮 + 启动/停止按钮 + 状态消息

SidePanel::left("subscription_list")    // 左侧订阅列表
  └─ ScrollArea
      └─ 可选择的订阅列表

CentralPanel::default()                  // 右侧主面板
  ├─ 连接配置卡片
  └─ 订阅详情卡片 或 空状态提示

Window::new("新建订阅")                   // 新建订阅弹窗（条件显示）
  └─ 表单字段 + 按钮
```

### 借用检查优化
通过引入 `delete_selected` 标志避免在闭包中同时可变借用 `sub` 和 `self`：

```rust
let mut delete_selected = false;
// ... 在闭包中设置标志 ...
if delete_selected {
    // 在闭包外执行删除操作
}
```

## 对比原版的改进

| 方面 | 原版 | 重构后 |
|------|------|--------|
| 布局 | 上下堆叠 | 左右分栏 |
| 状态栏 | 混在内容中间 | 顶部独立显示 |
| 订阅列表 | 折叠面板，展开后平铺 | 左侧固定可见列表 |
| 新建订阅 | 默认展开，占据大量空间 | 弹窗，按需显示 |
| 提示词输入 | 单行输入框 | 多行文本框（100px） |
| 详情查看 | 列表中展示，不可编辑 | 右侧面板，可编辑 |
| 操作按钮 | 平铺在底部 | 主次分明，固定底栏 |
| 空间利用 | 低（大量留白） | 高（合理分配） |
| 信息层级 | 不清晰 | 清晰（顶部-左侧-右侧-底部） |

## 使用流程

### 首次使用
1. 打开应用，看到顶部状态 "○ 已停止"
2. 右侧显示连接配置（WS地址 + 工作区）
3. 左侧显示 "暂无订阅" + 引导文字
4. 点击左上角 "+ 新建" 创建第一个订阅
5. 填写表单，点击 "✅ 创建"
6. 左侧列表出现新订阅，右侧显示详情
7. 点击底部 "▶ 启动服务"

### 日常使用
1. 左侧选择要编辑的订阅
2. 右侧查看/编辑详情
3. 修改后自动保存（或手动点击 "保存配置"）
4. 底部切换运行状态

## 编译和运行

```bash
cd rust
cargo build --release
../rust/target/release/agent-loop gui
```

## 后续优化方向

1. **引导流程**：
   - 首次启动显示配置向导
   - 步骤指示器（①配置连接 → ②添加订阅 → ③启动服务）

2. **视觉优化**：
   - 自定义主题色
   - 更大的间距（24px）
   - 圆角卡片
   - 阴影效果

3. **交互优化**：
   - 订阅拖拽排序
   - 批量操作（全选/删除）
   - 搜索/过滤订阅
   - 键盘快捷键

4. **功能增强**：
   - 日志查看面板
   - 实时运行状态（WebSocket连接状态、事件计数）
   - 配置导入/导出
   - 订阅模板

5. **响应式设计**：
   - 小窗口自动切换为上下布局
   - 折叠左侧面板按钮
   - 最小窗口尺寸限制

